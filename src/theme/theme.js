import { createTheme } from 'UI-Components-books'

export const theme = createTheme({
  palette: {
    primary: {
      200: '#daa375',
      300: '#edb17a',
      400: '#d18e55',
      800: '#763f1d',
      900: '#5e320e'
    }
  }
})
